# System Architecture - UI-Test App

## Overview
A minimal Flutter application created for testing UI components and cookbooks.

## Project Structure
- `lib/main.dart`: Entry point of the application.
- `android/`, `ios/`, `windows/`, `web/`, `linux/`, `macos/`: Platform-specific configurations.

## Dependencies
- Flutter SDK
- Standard Flutter packages
